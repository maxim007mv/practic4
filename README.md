# 🛒 GadgetStore — Интернет-магазин электроники

> **Практическое занятие 4** — API + React  
> Дисциплина: Фронтенд и бэкенд разработка  
> Семестр: 4 семестр, 2025/2026 уч. год  
> Институт: ИПТИП, кафедра Индустриального программирования

---

## 📋 Описание проекта

Полноценное веб-приложение интернет-магазина электроники и гаджетов, состоящее из двух частей:

- **Бэкенд** (Express.js) — REST API с CRUD-операциями для управления каталогом товаров
- **Фронтенд** (React) — SPA-интерфейс для просмотра, добавления, редактирования и удаления товаров

Приложения связаны через HTTP-запросы с помощью библиотеки **axios**, с настроенной политикой **CORS**.

---

## 📸 Скриншоты

### Главная страница — каталог товаров
![Каталог товаров](https://i.ibb.co/1f12TZCB/Screenshot-2026-02-17-at-16-14-42.png)

### Модальное окно — добавление товара
![Добавление товара](https://i.ibb.co/JwH41S4V/Screenshot-2026-02-17-at-16-15-04.png)

### Модальное окно — редактирование товара
![Редактирование товара](https://i.ibb.co/KcTkRNcY/Screenshot-2026-02-17-at-16-14-56.png)

### Фильтрация по категориям
![Фильтрация](https://i.ibb.co/YFHTrnGD/Screenshot-2026-02-17-at-16-15-30.png)



---

## 🗂 Структура проекта

```
practic4.2/
├── server/                          # Бэкенд (Express)
│   ├── app.js                       # Точка входа сервера, маршруты API
│   ├── package.json                 # Зависимости сервера
│   └── package-lock.json
│
├── client/                          # Фронтенд (React)
│   ├── public/
│   │   └── index.html               # HTML-шаблон
│   ├── src/
│   │   ├── api/
│   │   │   └── index.js             # API-клиент (axios)
│   │   ├── components/
│   │   │   ├── ProductItem.jsx      # Карточка товара
│   │   │   ├── ProductModal.jsx     # Модальное окно (создание/редактирование)
│   │   │   └── ProductsList.jsx     # Список товаров (сетка карточек)
│   │   ├── pages/
│   │   │   └── ProductsPage/
│   │   │       ├── ProductsPage.jsx # Главная страница каталога
│   │   │       ├── ProductsPage.scss# Стили (SCSS-исходник)
│   │   │       └── ProductsPage.css # Скомпилированные стили
│   │   ├── App.js                   # Корневой компонент
│   │   ├── index.js                 # Точка входа React
│   │   └── index.css                # Глобальные стили
│   ├── package.json                 # Зависимости клиента
│   └── package-lock.json
│
└── README.md                        # Этот файл — отчёт по проекту
```

---

## ⚙️ Технологии

| Часть       | Технология | Назначение |
|-------------|-----------|------------|
| **Бэкенд**  | Express.js | HTTP-сервер и маршрутизация |
|             | nanoid     | Генерация уникальных ID товаров |
|             | cors       | Настройка политики CORS |
| **Фронтенд**| React 18   | UI-библиотека для построения интерфейса |
|             | axios      | HTTP-клиент для запросов к API |
|             | Sass (SCSS)| CSS-препроцессор для стилизации |

---

## 🚀 Запуск проекта

### 1. Клонировать репозиторий

```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd practic4.2
```

### 2. Установить зависимости

```bash
# Бэкенд
cd server
npm install

# Фронтенд
cd ../client
npm install
```

### 3. Запустить сервер (порт 3000)

```bash
cd server
node app.js
```

В консоли появится:
```
Сервер запущен на http://localhost:3000
```

### 4. Запустить клиент (порт 3001)

В **новом** терминале:

```bash
cd client
npm run dev
```

Откроется браузер с адресом `http://localhost:3001`.

---

## 🔌 API — Маршруты бэкенда

Базовый URL: `http://localhost:3000/api`

| Метод    | Путь                | Описание                       |
|----------|---------------------|--------------------------------|
| `GET`    | `/api/products`     | Получить список всех товаров   |
| `GET`    | `/api/products/:id` | Получить товар по ID           |
| `POST`   | `/api/products`     | Создать новый товар            |
| `PATCH`  | `/api/products/:id` | Обновить товар (частично)      |
| `DELETE` | `/api/products/:id` | Удалить товар                  |

### Пример тела запроса (POST / PATCH)

```json
{
  "name": "iPhone 15 Pro",
  "category": "Смартфоны",
  "description": "Флагманский смартфон Apple с чипом A17 Pro",
  "price": 89990,
  "stock": 25,
  "rating": 4.8,
  "image": "https://example.com/iphone.jpg"
}
```

---

## 📦 Что было сделано

### Бэкенд (`server/app.js`)

1. **Создан Express-сервер** с REST API для управления товарами
2. **12 начальных товаров** по тематике электроники и гаджетов (смартфоны, ноутбуки, наушники, планшеты, часы, аксессуары, аудио)
3. **Middleware для логирования** — каждый запрос логируется в консоль с временной меткой, методом, статусом и путём
4. **Middleware для CORS** — разрешены запросы с фронтенда (`http://localhost:3001`)
5. **Валидация данных** — проверка обязательных полей при создании и обновлении товаров
6. **Функция `findProductOr404()`** — вспомогательная функция для поиска товара с автоматическим ответом 404
7. **Обработка ошибок** — catch-all маршрут для 404 и глобальный обработчик ошибок 500
8. **nanoid** — генерация уникальных 6-символьных идентификаторов вместо числовых ID

### Фронтенд (`client/`)

1. **React-приложение** создано с помощью Create React App
2. **Компонентная архитектура:**
   - `ProductsPage.jsx` — главная страница с логикой загрузки, фильтрации, управления модалкой
   - `ProductsList.jsx` — отображение сетки карточек товаров
   - `ProductItem.jsx` — карточка одного товара с фото, ценой, рейтингом, количеством на складе
   - `ProductModal.jsx` — модальное окно для создания и редактирования товара с валидацией полей
3. **API-клиент** (`src/api/index.js`) — обёртка над axios с настроенным `baseURL` и заголовками
4. **Поиск** — фильтрация товаров по названию и описанию в реальном времени
5. **Фильтр по категориям** — выпадающий список с динамически формируемыми категориями
6. **Счётчик** — отображение количества найденных товаров

### Стилизация

1. **SCSS** (`ProductsPage.scss`) — исходный файл с переменными, миксинами, вложенностью
2. **CSS** (`ProductsPage.css`) — скомпилированный файл стилей
3. **Тёмная тема** — фон `#0b0f19`, цвет текста `#e7eaf3`
4. **Карточки товаров** — CSS Grid с адаптивной сеткой (`auto-fill, minmax(300px, 1fr)`)
5. **Модальное окно** — backdrop с размытием, анимации, scrollable при большом количестве полей
6. **Адаптивность** — корректное отображение на мобильных устройствах (`@media max-width: 640px`)

### Связь фронтенда и бэкенда

1. **axios** — HTTP-клиент для всех запросов к API
2. **CORS** — настроен на сервере для разрешения запросов с порта 3001
3. **Все CRUD-операции** работают через API:
   - Загрузка списка при открытии страницы (`GET /api/products`)
   - Добавление товара через модалку (`POST /api/products`)
   - Редактирование товара через модалку (`PATCH /api/products/:id`)
   - Удаление товара с подтверждением (`DELETE /api/products/:id`)

---

## 🏷 Карточка товара содержит

| Поле        | Описание                               |
|-------------|----------------------------------------|
| `name`      | Название товара                        |
| `category`  | Категория (Смартфоны, Ноутбуки и т.д.) |
| `description` | Описание товара                      |
| `price`     | Цена в рублях                          |
| `stock`     | Количество на складе                   |
| `rating`    | Рейтинг (0–5, с отображением звёзд ★) |
| `image`     | Ссылка на фото товара                  |

---

## 📝 Примечания

- Сервер хранит данные **в оперативной памяти** (при перезапуске данные сбрасываются к начальным 12 товарам)
- Фронтенд запускается на порту **3001**, бэкенд — на порту **3000**
- Для генерации CSS из SCSS используется команда `npm run sass`
- Команда `npm run dev` запускает и Sass-компиляцию, и React-сервер одновременно
# practic4
